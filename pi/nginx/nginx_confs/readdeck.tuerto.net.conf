log_format readdeckformat '$http_x_forwarded_for $remote_addr [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent" $upstream_response_time';

server {
    server_name www.readdeck.tuerto.net;

    location / {
        proxy_pass http://192.168.0.95:8666/;
        proxy_set_header   Host $http_host;
	proxy_set_header   X-Real-IP $remote_addr;
	proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_read_timeout 1200s;
	proxy_connect_timeout	75s;
	proxy_http_version 1.1;
	proxy_set_header X-Forwarded-Proto $scheme;
	proxy_set_header Forwarded "for=$remote_addr;proto=$scheme";
	proxy_set_header Connection "";
	# used for view/edit office file via Office Online Server
	client_max_body_size 0;
	access_log      /var/log/nginx/readdeck.access.log readdeckformat;
	error_log       /var/log/nginx/readdeck.error.log;
    }


    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/tuerto.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/tuerto.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}
server {
    server_name readdeck.tuerto.net;

    # Redirect bare domain to /admin
    location = / {
        return 301 /admin;
    }

    location /admin/ {
        proxy_pass http://192.168.0.95:8666/admin/;
        proxy_set_header   Host $http_host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 1200s;
        proxy_connect_timeout   75s;
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Forwarded "for=$remote_addr;proto=$scheme";
        proxy_set_header Connection "";
        # used for view/edit office file via Office Online Server
        client_max_body_size 0;
        access_log      /var/log/nginx/readdeck.access.log readdeckformat;
        error_log       /var/log/nginx/readdeck.error.log;
    }

    location / {
        proxy_pass http://192.168.0.95:8666/;
        proxy_set_header   Host $http_host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 1200s;
        proxy_connect_timeout   75s;
        proxy_http_version 1.1;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Forwarded "for=$remote_addr;proto=$scheme";
        proxy_set_header Connection "";
        # used for view/edit office file via Office Online Server
        client_max_body_size 0;
        access_log      /var/log/nginx/readdeck.access.log readdeckformat;
        error_log       /var/log/nginx/readdeck.error.log;
    }



    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/tuerto.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/tuerto.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}


server {
    if ($host = readdeck.tuerto.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    server_name readdeck.tuerto.net;
    listen 80;
    return 404; # managed by Certbot


}server {
    if ($host = www.readdeck.tuerto.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    server_name www.readdeck.tuerto.net;
    listen 80;
    return 404; # managed by Certbot


}
